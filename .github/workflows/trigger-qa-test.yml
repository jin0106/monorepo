name: Trigger QA Jenkins Job

on:
  workflow_dispatch:
  workflow_call:

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job

        # 현재는 neubie-go만 qa test 진행중입니다. 추후 다른 app이 추가되면 input에서 app을 받는걸로 변경해야합니다.
        run: |
          echo "Triggering Jenkins..."
