name: Trigger A Jenkins Job

on:
  workflow_dispatch:


jobs:
  call-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job in A
        run: |
          curl -X POST "http://neubilitydev.iptime.org:58080/job/Neubie-go/build?token=platform_dev_jenkins_token"